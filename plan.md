# tulipindicators Rust 风格重构计划

## 目标

把当前的 `tulipindicators` 从典型 C 风格技术指标库，逐步重构为更接近 Rust 风格的工程结构与实现方式。

这里的“Rust 风格”不只是把语法翻译成 Rust，而是整体采用更清晰的模块边界、强类型接口、显式错误处理、可组合实现和可验证的迁移流程。

本次改造直接在当前 `tulipindicators` 目录内进行。
`tulipindicators2` 仅作为备份，不参与开发。

## 现状判断

当前工程的主要特点：

- 核心 API 由 `indicators.h` 和 `indicators.c` 暴露，采用全局注册表和弱类型函数指针。
- 指标实现分散在 `indicators/` 与 `beta/`，整体是以 C 文件平铺组织。
- 大量元信息通过生成文件维护，维护成本高，不利于演进。
- 接口以裸指针、长度参数、输出缓冲区为主，缺少更明确的数据约束。
- 测试主入口为 `smoke.c`，配合 `tests/` 中的文本数据做 golden 校验，这部分应尽量复用。

## 重构原则

- 不做一次性推翻重写，采用分阶段迁移。
- 先搭骨架，再迁移代表性指标，再批量推进。
- 先保持指标计算语义不变，再改善实现风格。
- 优先复用现有测试数据，避免迁移期间引入大量行为漂移。
- 迁移期允许旧实现与新实现并存，但必须有清晰边界。

## 目标架构

建议将项目重组为 Rust crate 形态，形成以下分层：

- `Cargo.toml`
- `src/lib.rs`
- `src/core/error.rs`
- `src/core/types.rs`
- `src/core/indicator.rs`
- `src/registry.rs`
- `src/stream.rs`
- `src/indicators/overlay/*.rs`
- `src/indicators/indicator/*.rs`
- `src/indicators/math/*.rs`
- `src/indicators/simple/*.rs`
- `tests/golden/*.rs`

设计目标：

- 用模块代替当前 C 文件平铺式组织。
- 用结构化元数据代替大型生成式注册表。
- 用切片、结构体、枚举错误替代裸指针和整数错误码。
- 用 trait 或统一函数签名表达指标能力。
- 用明确状态对象替代当前弱类型流式接口。

## 目标接口设计

### 1. 核心类型

需要定义统一的数据模型，例如：

- 输入切片：`&[f64]`
- 多输入容器：`IndicatorInputs`
- 参数容器：`IndicatorOptions`
- 输出容器：`IndicatorOutputs`
- 元信息：`IndicatorMetadata`

### 2. 错误模型

需要引入显式错误类型，例如：

- `InvalidOption`
- `InputLengthMismatch`
- `OutputTooSmall`
- `NotEnoughData`
- `UnknownIndicator`

避免继续依赖 `TI_OKAY`、`TI_INVALID_OPTION` 这一类整数错误码作为主接口。

### 3. 指标抽象

建议定义统一的指标接口，至少覆盖：

- 元信息访问
- `lookback` 计算
- 批处理计算
- 可选的流式计算能力

对于只支持批处理的指标，不强行抽象成复杂状态机。
对于 `sma`、`atr` 这类已有流式语义的指标，保留状态对象接口。

### 4. 注册表

当前 `ti_indicators[]` 属于 C 风格静态全局表。
Rust 风格下应改为：

- 明确的注册模块
- 类型安全的元信息
- 按名称查询的只读索引
- 不依赖代码生成的大型单文件拼接

## 迁移策略

### 阶段 1：建立 Rust 工程骨架

目标：

- 在当前仓库内加入 Cargo 工程
- 保持旧 C 代码不删除，作为对照基线
- 先不追求全量功能可用

工作项：

- 新建 `Cargo.toml`
- 新建 `src/lib.rs` 和核心模块目录
- 定义基础错误类型、元信息结构、指标 trait
- 建立最小注册表框架

完成标准：

- `cargo check` 可以通过
- 可以注册并调用最小示例指标

### 阶段 2：迁移最小代表指标集

建议首批迁移：

- `sma`
- `ema`
- `bbands`
- `rsi`
- `macd`
- `atr`

选择理由：

- 覆盖单输入单输出
- 覆盖多输出
- 覆盖窗口类算法
- 覆盖递推状态类算法
- 覆盖流式场景
- 覆盖依赖内部子计算的组合型指标

完成标准：

- 这批指标的新实现可以独立运行
- 结果与现有 golden data 一致
- 流式指标的批处理与分步运行结果一致

### 阶段 3：接入迁移期测试体系

目标：

- 复用现有 `tests/` 文本数据
- 为 Rust 实现建立 golden test
- 保留旧 C 行为作为回归基准

工作项：

- 编写数据加载测试工具
- 让 Rust 测试读取现有测试样本
- 对比 lookback、输出长度、逐项数值结果
- 覆盖非法参数和边界输入

完成标准：

- `cargo test` 可跑通已迁移指标
- 已迁移指标有明确回归基准

### 阶段 4：按类别批量迁移稳定指标

迁移顺序建议：

1. `simple`
2. `math`
3. 常见 `overlay`
4. 常见 `indicator`
5. 多输入多输出复杂指标
6. 流式专用和 reference 实现
7. `beta` 指标

原因：

- `simple` 和 `math` 结构最规则，适合先统一风格和模板。
- 常见均线和窗口指标能尽快建立稳定实现模式。
- 复杂指标留到骨架和测试成熟后处理，降低返工概率。
- `beta` 最后处理，避免不稳定接口拖慢主线。

完成标准：

- 主体稳定指标完成 Rust 化
- 注册表不再依赖旧 C 风格元数据拼接

### 阶段 5：处理兼容层与收尾

这一阶段要明确最终目标：

- 如果目标是纯 Rust 库，则删除旧 C 入口和生成脚本依赖。
- 如果仍需兼容 C API，则保留一个薄 FFI 层，把旧接口映射到 Rust 内核。

工作项：

- 清理生成式注册逻辑
- 清理不再使用的模板和脚本
- 统一 README、示例、测试入口
- 明确是否保留 `Makefile` 和 `tiamalgamation.c`

## 需要重点处理的技术问题

### 1. 输入输出所有权

当前接口允许调用方直接提供输入数组和输出缓冲区。
Rust 风格下需要明确：

- 输入是否必须等长
- 输出由库分配还是调用方提供
- 多输出指标如何表达
- 长度不足时如何返回错误

### 2. lookback 与输出长度

当前很多指标隐式依赖：

- `output_size = input_size - start(options)`

Rust 化后应把这个规则显式化，避免调用方自己拼长度计算。

### 3. 流式接口设计

当前 `ti_stream` 的抽象偏弱，只保存索引和进度，再转发到函数表。
Rust 版本应改成显式状态对象：

- 每个支持流式的指标有自己的状态结构
- 状态推进和输出行为可测试
- 批处理与流式语义保持一致

### 4. 数值一致性

迁移过程中要避免因为：

- 初始化细节
- 浮点舍入
- lookback 定义
- 边界窗口处理

导致行为与旧版 silently 偏离。

### 5. 元数据维护方式

当前元数据集中在大型生成文件里。
Rust 风格下更适合：

- 每个指标自带元信息
- 由注册模块统一汇总
- 尽量减少“改一个指标要同步改多个生成文件”的情况

## 验证策略

每个阶段都要做实际验证，不接受只改结构不校验行为。

建议验证项：

- `cargo check`
- `cargo test`
- 已迁移指标对现有 `tests/` 数据做 golden 对比
- 批处理与流式结果对比
- 非法参数测试
- 输入长度不匹配测试

迁移中如果某个指标暂未纳入测试，应在阶段记录中明确标注，而不是默认视为完成。

## 建议的提交节奏

为了让历史可读，建议按以下粒度提交：

1. 建立 Cargo 骨架和核心抽象
2. 接入测试数据加载与 golden test 框架
3. 迁移 `sma` 和 `ema`
4. 迁移 `bbands`、`rsi`
5. 迁移 `macd`、`atr`
6. 按类别批量迁移剩余稳定指标
7. 处理兼容层与清理旧结构

每个提交都应说明：

- 为什么做这一步
- 实际迁移了什么
- 跑了哪些验证
- 哪些内容明确还没处理

## 第一批实际落地建议

建议下一步直接做以下内容：

- 在当前目录加入 Cargo 工程骨架
- 定义 `Error`、`Metadata`、`Indicator` 抽象
- 建立最小注册表
- 先迁移 `sma` 和 `ema`
- 让 Rust 测试跑通这两个指标的 golden data

这样可以尽快验证：

- 架构是否合适
- 接口是否足够表达后续指标
- 测试数据复用是否顺畅
- 后续批量迁移是否具备可复制模板

## 暂不包含

本计划当前不包含：

- 一次性迁移全部 104 个指标
- 立刻删除旧 C 实现
- 立刻重写文档站点或 bindings
- 立刻处理全部 `beta` 指标
- 立刻设计最终对外 FFI 稳定接口

这些内容应在主干架构和首批指标稳定后再决定。
